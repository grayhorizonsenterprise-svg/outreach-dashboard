[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python start.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Set GRANT_AGENT_URL in Railway dashboard to the live grant agent service URL
# e.g. https://ghe-grant-agent.up.railway.app
