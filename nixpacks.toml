[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r edge_engine/requirements.txt"]

[start]
cmd = "cd edge_engine && gunicorn dashboard:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120"
